[phases.setup]
nixPkgs = ["php82", "php82Extensions.pdo", "php82Extensions.pdo_mysql", "php82Extensions.mysqli"]

[phases.install]
cmds = ["cd thyro_sight && composer install --no-dev --optimize-autoloader || true"]

[start]
cmd = "php -S 0.0.0.0:${PORT:-8080} -t thyro_sight"
